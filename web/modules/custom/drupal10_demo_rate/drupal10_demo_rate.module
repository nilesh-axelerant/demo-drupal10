<?php

use Drupal\comment\CommentInterface;

/**
 * Implements hook_comment_insert()
 */
function drupal10_demo_rate_comment_insert(CommentInterface $comment) {
  \Drupal::logger('custom::')->notice('<pre><code>' . print_r("comes", TRUE) . '</code></pre>');
}
